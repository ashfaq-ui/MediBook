# Default dev target: hosted backend (so hosted mock users can log in)
VITE_API_BASE_URL=https://medibook-production-3b64.up.railway.app/api

# Local backend option (uncomment this and comment the line above when needed)
# VITE_API_BASE_URL=http://localhost:8080/api
